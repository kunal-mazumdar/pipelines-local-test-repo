pipelines:
  - name: my_first_pipeline
    configuration:
      environmentVariables:
        readOnly:
          XRAY_ENABLED: "true"
    steps:
      - name: my_first_step
        type: Bash
        execution:
          onExecute:
            - echo "test"
            - {{ ternary "true" "false" ( hasPrefix "master" "{{gitBranch}}" ) }}
