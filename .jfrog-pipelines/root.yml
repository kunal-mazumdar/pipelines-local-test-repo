resources:
  - name: myFirstRepo
    type: GitRepo
    configuration:
      gitProvider: mygithub
      path: {{sourceValueRepository}}
      branches:
        include: 'master'

pipelines:
  - name: Ps_variable_resolved_6730
    steps:
      - name: Ps_variable_resolved_6730
        type: Bash
        configuration:
          environmentVariables:
            env2: {{sourceValueRepository}}
        execution:
          onExecute:
            - echo {{sourceValueRepository}}
            - cd /tmp
