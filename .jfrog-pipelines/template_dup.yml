template:
  namespace: local
  name: template1
  version: 0.0.0

pipelines:
  - name: template_pipeline_dup
    steps:
      - name: template_step_dup
        type: Bash
        execution:
          onExecute:
            - echo "test from template"